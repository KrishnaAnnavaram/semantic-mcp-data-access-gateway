"""The two MCP servers and the host that drives them.

The package is `mcp_servers`, deliberately NOT `mcp` -- that name belongs to the
MCP SDK on PyPI, and shadowing it breaks every server with an import error that
looks like a corrupted install."""
